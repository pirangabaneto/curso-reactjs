//import { render, screen } from '@testing-library/react';
//import Home from '.';

describe('<Home />', () => {
  it('test one', () => {
    expect(1).toBe(1);
  });

  it('test two', () => {
    expect(1).toBe(1);
  });

  it('test three', () => {
    expect(1).toBe(1);
  });

});



